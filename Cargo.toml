[workspace]

members = [
    "screen_capture",
    "displaylight",
    "lights",
    "firmware",
]


[profile.release]
opt-level = 3

[profile.firmware]
inherits = "release"
codegen-units = 1
debug = true
lto = true
